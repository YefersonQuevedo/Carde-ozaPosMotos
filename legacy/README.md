# Prototipo V2 (legado)

Estos archivos (`index.html`, `app.js`, `styles.css`, `server.js`) son el **prototipo
original** que guardaba todo en `localStorage`. Se conservan solo como referencia.

La version activa es **`backend/` (Node + Express + Prisma + MySQL)** + **`frontend/`
(wizard de venta progresivo)**. Ver el `README.md` de la raiz.
